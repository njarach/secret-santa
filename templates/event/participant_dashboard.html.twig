{% extends 'base.html.twig' %}

{% block body %}
    <div class="min-h-screen py-8 px-4">
        <div class="max-w-2xl mx-auto">
            <!-- Messages flash -->
            <div id="flash-messages" class="mb-4">
                {% include 'event/htmx_partials/_participant_flash_messages.html.twig' %}
            </div>

            <!-- En-tête -->
            <div class="text-center mb-8">
                <h1 class="font-bold text-white mb-2">🎁 {{ event.name }}</h1>
                <p class="text-white">Bienvenue {{ participant.name }} !</p>
                {% if event.budget %}
                    <p class="text-white text-sm mt-2">Budget suggéré : {{ event.budget|number_format(2, ',', ' ') }} €</p>
                {% endif %}
            </div>

            <!-- Mon profil -->
            <div class="bg-white rounded-lg shadow-lg mb-6">
                <div class="bg-gradient-to-r from-green-600 to-green-700 px-6 py-3">
                    <h2 class="text-lg font-bold">👤 Mon profil</h2>
                </div>
                <div class="p-6">
                    <form action="{{ path('app_participant_update_name', {'id': participant.id}) }}" method="post" hx-boost="true">
                        <label class="block text-sm font-medium text-gray-700 mb-2">Nom</label>
                        <div class="flex gap-2">
                            <input type="text"
                                   name="name"
                                   value="{{ participant.name }}"
                                   required
                                   class="flex-1 px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-green-500 focus:border-green-500">
                            <button type="submit"
                                    class="bg-red-600 hover:bg-red-700 text-white px-6 py-2 rounded-lg font-medium transition-colors">
                                Modifier
                            </button>
                        </div>
                        <p class="text-gray-500 text-xs mt-2">📧 Email : {{ participant.email }}</p>
                    </form>
                </div>
            </div>

            <!-- Liste de souhaits -->
            <div class="bg-white rounded-lg shadow-lg mb-6">
                <div class="bg-gradient-to-r from-red-600 to-red-700 px-6 py-3">
                    <h2 class="text-lg font-bold">🎅 Ma liste de souhaits</h2>
                </div>
                <div class="p-6">
                    <form action="{{ path('app_participant_update_wishlist', {'id': participant.id}) }}" method="post" hx-boost="true">
                        <p class="text-gray-600 text-sm mb-3">
                            Donne des idées pour t'aider à choisir le cadeau parfait !
                        </p>
                        <textarea name="wishlist"
                                  rows="4"
                                  placeholder="Ex: Un livre, des chaussettes rigolotes, un jeu de société..."
                                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-red-500 focus:border-red-500 resize-none">{{ participant.wishlist }}</textarea>
                        <button type="submit"
                                class="mt-3 bg-red-600 hover:bg-red-700 text-white px-6 py-2 rounded-lg font-medium transition-colors w-full">
                            💾 Enregistrer
                        </button>
                    </form>
                </div>
            </div>

            <!-- Tirage -->
            {% if participant.draw %}
                <div class="bg-white rounded-lg shadow-lg mb-6">
                    <div class="bg-gradient-to-r from-purple-600 to-purple-700 px-6 py-3">
                        <h2 class="text-lg font-bold">🎯 Mon tirage</h2>
                    </div>
                    <div class="p-6">
                        <p class="text-gray-600 text-center mb-3">Tu dois offrir un cadeau à :</p>
                        <p class="text-2xl font-bold text-purple-600 text-center mb-4">{{ participant.draw.receiver.name }}</p>
                        {% if participant.draw.receiver.wishlist %}
                            <div class="bg-purple-50 rounded-lg p-4">
                                <p class="text-sm font-semibold text-purple-900 mb-2">💡 Sa liste de souhaits :</p>
                                <p class="text-gray-700 text-sm whitespace-pre-line">{{ participant.draw.receiver.wishlist }}</p>
                            </div>
                        {% else %}
                            <p class="text-gray-500 text-sm text-center italic">Aucune liste de souhaits fournie</p>
                        {% endif %}
                    </div>
                </div>
            {% else %}
                <div class="bg-white rounded-lg shadow-lg mb-6">
                    <div class="bg-gradient-to-r from-gray-600 to-gray-700 px-6 py-3">
                        <h2 class="text-lg font-bold">⏳ Tirage en attente</h2>
                    </div>
                    <div class="p-6 text-center">
                        <div class="inline-flex items-center justify-center w-16 h-16 mb-3 bg-gray-100 rounded-full">
                            <svg class="w-8 h-8 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                            </svg>
                        </div>
                        <p class="text-gray-600 font-medium">Le tirage au sort n'a pas encore été effectué</p>
                        <p class="text-gray-500 text-sm mt-1">L'administrateur lancera le tirage bientôt !</p>
                    </div>
                </div>
            {% endif %}

            <!-- Quitter -->
            <div class="bg-white rounded-lg shadow-lg">
                <div class="p-6">
                    <form hx-post="{{ path('app_event_leave', {'id': event.id}) }}"
                          hx-confirm="Es-tu sûr de vouloir quitter cet événement ?">
                        <button type="submit"
                                class="w-full bg-red-600 hover:bg-red-700 text-white px-6 py-3 rounded-lg font-medium transition-colors flex items-center justify-center gap-2">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"/>
                            </svg>
                            Quitter l'événement
                        </button>
                    </form>
                </div>
            </div>

            <!-- Retour -->
            <div class="text-center mt-6">
                <a href="{{ path('app_home') }}" class="text-gray-300 hover:text-white transition-colors text-sm">
                    ← Retour à l'accueil
                </a>
            </div>
        </div>
    </div>
{% endblock %}

{% block title %}Dashboard - {{ event.name }}{% endblock %}
