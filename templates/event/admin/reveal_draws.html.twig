{% extends 'base.html.twig' %}

{% block body %}
    <div class="min-h-screen py-6 px-4 sm:px-6 lg:px-8">
        <div class="max-w-4xl mx-auto">
            <!-- Avertissement -->
            <div class="bg-red-50 border-l-4 border-red-600 p-6 mb-6 rounded-lg">
                <div class="flex">
                    <div class="flex-shrink-0">
                        <svg class="h-8 w-8 text-red-600" fill="currentColor" viewBox="0 0 20 20">
                            <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd"/>
                        </svg>
                    </div>
                    <div class="ml-3">
                        <h3 class="text-lg font-bold text-red-800">⚠️ Attention Spoilers !</h3>
                        <p class="mt-2 text-red-700">
                            Cette page révèle tous les tirages. Si tu participes aussi à cet événement, tu vas découvrir qui doit t'offrir un cadeau.
                            <strong>Es-tu sûr de vouloir gâcher la surprise ?</strong>
                        </p>
                    </div>
                </div>
            </div>

            <!-- En-tête -->
            <div class="bg-white rounded-lg shadow-lg overflow-hidden mb-6">
                <div class="bg-gradient-to-r from-red-600 to-red-700 px-6 py-4">
                    <h1 class="text-2xl font-bold text-white">🔍 Révélation des tirages</h1>
                    <p class="text-white mt-1">{{ event.name }}</p>
                </div>
            </div>

            <!-- Liste des tirages -->
            <div class="bg-white rounded-lg shadow-lg overflow-hidden">
                <div class="bg-gray-50 px-6 py-4 border-b border-gray-200">
                    <h2 class="text-xl font-bold text-gray-900">🎁 Résultats du tirage</h2>
                </div>

                <div class="divide-y divide-gray-200">
                    {% if draws is empty %}
                        <div class="px-6 py-8 text-center text-gray-500">
                            <p>Aucun tirage n'a encore été effectué.</p>
                        </div>
                    {% else %}
                        {% for draw in draws %}
                            <div class="px-6 py-4 hover:bg-gray-50 transition-colors">
                                <div class="flex items-center justify-between">
                                    <div class="flex items-center space-x-4">
                                        <div class="flex-shrink-0">
                                            <div class="w-10 h-10 bg-gradient-to-br from-green-400 to-green-600 rounded-full flex items-center justify-center text-white font-bold">
                                                {{ draw.giver.name|slice(0, 1)|upper }}
                                            </div>
                                        </div>
                                        <div>
                                            <p class="text-sm font-medium text-gray-900">{{ draw.giver.name }}</p>
                                            <p class="text-xs text-gray-500">{{ draw.giver.email }}</p>
                                        </div>
                                    </div>

                                    <div class="flex items-center space-x-3">
                                        <svg class="w-6 h-6 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"/>
                                        </svg>
                                    </div>

                                    <div class="flex items-center space-x-4">
                                        <div>
                                            <p class="text-sm font-medium text-gray-900 text-right">{{ draw.receiver.name }}</p>
                                            <p class="text-xs text-gray-500 text-right">{{ draw.receiver.email }}</p>
                                        </div>
                                        <div class="flex-shrink-0">
                                            <div class="w-10 h-10 bg-gradient-to-br from-red-400 to-red-600 rounded-full flex items-center justify-center text-white font-bold">
                                                {{ draw.receiver.name|slice(0, 1)|upper }}
                                            </div>
                                        </div>
                                    </div>
                                </div>

                                {% if draw.receiver.wishlist %}
                                    <div class="mt-3 ml-14 bg-purple-50 rounded-lg p-3">
                                        <p class="text-xs font-semibold text-purple-900 mb-1">💡 Liste de souhaits :</p>
                                        <p class="text-xs text-gray-700 whitespace-pre-line">{{ draw.receiver.wishlist }}</p>
                                    </div>
                                {% endif %}
                            </div>
                        {% endfor %}
                    {% endif %}
                </div>
            </div>

            <!-- Retour -->
            <div class="mt-6 text-center">
                <a href="{{ path('app_event_admin_dashboard', {id: event.id}) }}"
                   class="inline-flex items-center text-white hover:text-gray-300 transition-colors">
                    <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
                    </svg>
                    Retour au dashboard
                </a>
            </div>
        </div>
    </div>
{% endblock %}

{% block title %}Révélation des tirages - {{ event.name }}{% endblock %}
