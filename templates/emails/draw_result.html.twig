{% extends 'base.html.twig' %}

{% block body %}
    <section class="max-w-2xl px-6 py-8 mx-auto bg-white dark:bg-gray-900">
        <header>
            <a href="#">
                <img class="w-auto h-7 sm:h-8" src="{{ absolute_url(asset('images/santa-face.png')) }}" alt="Visage de père noël">
            </a>
        </header>

        <main class="mt-8">
            <h2 class="text-2xl font-bold text-gray-700 dark:text-gray-200">🎄 Le tirage a été effectué !</h2>

            <p class="mt-4 leading-loose text-gray-600 dark:text-gray-300">
                Bonjour <strong>{{ participant.name }}</strong>,
            </p>

            <p class="mt-2 leading-loose text-gray-600 dark:text-gray-300">
                Le tirage au sort pour l'événement <strong>{{ event.name }}</strong> a été réalisé !
            </p>

            <hr class="my-6 border-gray-200 dark:border-gray-700">

            <div class="bg-gradient-to-r from-red-50 to-green-50 dark:from-red-900/20 dark:to-green-900/20 rounded-lg p-6">
                <h3 class="text-xl font-bold text-center text-gray-800 dark:text-gray-200 mb-4">
                    🎁 Tu dois offrir un cadeau à :
                </h3>
                <p class="text-3xl font-bold text-center text-red-600 dark:text-red-400 mb-4">
                    {{ receiver.name }}
                </p>

                {% if event.budget %}
                    <p class="text-center text-gray-600 dark:text-gray-400 mt-4">
                        💰 Budget suggéré : <strong>{{ event.budget|number_format(2, ',', ' ') }} €</strong>
                    </p>
                {% endif %}
            </div>

            <div class="mt-6 text-center">
                <a href="{{ participantAccessUrl }}"
                   class="inline-block bg-red-600 hover:bg-red-700 text-white font-medium py-3 px-6 rounded-lg transition-colors">
                    Voir mon espace participant
                </a>
            </div>

            <div class="mt-6 p-4 bg-yellow-50 dark:bg-yellow-900/20 rounded-lg">
                <p class="text-sm text-gray-600 dark:text-gray-300">
                    ⚠️ <strong>Important :</strong> Garde cette information secrète ! C'est tout l'intérêt du Secret Santa 🤫
                </p>
            </div>
        </main>

        <footer class="mt-8">
            <p class="mt-3 text-gray-500 dark:text-gray-400">© 2025 Nicolas Jarach Dev, Secret Santa.</p>
        </footer>
    </section>
{% endblock %}
